apiVersion: v1
kind: Service
metadata:
  name: {{ include "orbit.fullname" . }}-frontend
  labels:
    {{- include "orbit.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orbit.frontend.selectorLabels" . | nindent 4 }}
