# =============================================================================
# Platform Database Module - Provider Requirements
# =============================================================================

terraform {
  required_version = ">= 1.5.0"

  # Note: Provider configurations are passed in from the root module.
  # This module does not configure providers directly.
}
