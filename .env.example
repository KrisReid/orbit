# Orbit Environment Configuration
# Copy this file to .env and customize for your deployment
# Run ./install.sh to generate secure secrets automatically

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Domain Configuration
# The domain where Orbit will be accessible (e.g., orbit.mycompany.com)
# For local testing, use: localhost
DOMAIN=localhost

# Email for Let's Encrypt TLS certificates
# Required for production deployments with HTTPS
ACME_EMAIL=admin@example.com

# =============================================================================
# SECURITY (Auto-generated by install.sh - DO NOT use defaults in production)
# =============================================================================

# Secret key for JWT token signing - MUST be unique and secure
# Generate with: openssl rand -base64 32
SECRET_KEY=CHANGE_ME_GENERATE_SECURE_KEY

# PostgreSQL password - MUST be unique and secure
# Generate with: openssl rand -base64 16
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL settings
POSTGRES_USER=orbit
POSTGRES_DB=orbit

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Task ID prefix (appears in task identifiers like ORBIT-123)
TASK_ID_PREFIX=ORBIT

# Debug mode - set to false in production
DEBUG=false

# =============================================================================
# OPTIONAL: GITHUB INTEGRATION
# =============================================================================

# GitHub webhook secret for PR/commit linking
# GITHUB_WEBHOOK_SECRET=

# GitHub App credentials (for advanced integration)
# GITHUB_APP_ID=
# GITHUB_PRIVATE_KEY=

# =============================================================================
# ADVANCED SETTINGS (usually no need to change)
# =============================================================================

# Image version tag (default: latest)
# ORBIT_VERSION=latest

# Image registry prefix (leave empty to build locally)
# For pre-built images from GitHub Container Registry:
# IMAGE_REGISTRY=ghcr.io/your-org/orbit-
# This will result in: ghcr.io/your-org/orbit-backend:latest
# IMAGE_REGISTRY=

# CORS origins (comma-separated) - auto-configured based on DOMAIN
# CORS_ORIGINS=https://orbit.mycompany.com
